Apache Maven 3.0.4
$ mvn archetype:generate -DgroupId=best.purchase -DartifactId=purchase-best-price -DarchetypeArtifactId=maven-archetype-quickstart -DinteractiveMode=false

Project to get quotes from merchants in threads and buy products at the best price.
